import { Link, Outlet } from "react-router";
import { Navigation } from "~/Navigation";

export default function Page() {
    return <div>
        <header>/account.demo.tsx</header>
        <hr />
        <Navigation />
    </div>
}
